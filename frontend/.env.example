# Backend API URL (the FastAPI app, exposed on :8080 in Docker, :8001 locally)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8001

# Simulation engine URL (direct access, exposed on :8000)
NEXT_PUBLIC_ENGINE_URL=http://localhost:8000
